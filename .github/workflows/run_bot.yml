- name: Verify Secret
  run: |
    if [ -z "$DISCORD_WEBHOOK" ]; then
      echo "❌ ERROR: DISCORD_WEBHOOK is empty!"
    else
      echo "✅ Webhook secret is set (first 10 chars: ${DISCORD_WEBHOOK:0:10}...)"
    fi
  env:
    DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
